# Repository guidelines

## Deletion Policy — NEVER permanently delete

Never `rm`/permanently delete any file. Move it to `~/Documents/Github/_archive/` instead so it can be recovered. No exceptions.
